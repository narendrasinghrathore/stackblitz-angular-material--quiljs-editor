import { FontHugeDirective } from './font-huge.directive';

describe('FontHugeDirective', () => {
  it('should create an instance', () => {
    const directive = new FontHugeDirective();
    expect(directive).toBeTruthy();
  });
});
